#
# This file is automatically generated by the Terraform inventory module
#
---
all:
  hosts:
%{ for cluster, hostinfo in rally_nodes ~}%{ for index, ip in hostinfo.ips ~}
    ${ip}:
      hostname_short: ${ hostinfo.hostnames_short[index] }
      hostname_long: ${ hostinfo.hostnames_long[index] }
%{ endfor ~}%{ endfor ~}
%{ for cluster, hostinfo in es_nodes ~} %{ for index, ip in hostinfo.ips ~}
    ${ip}:
      hostname_short: ${ hostinfo.hostnames_short[index] }
      hostname_long: ${ hostinfo.hostnames_long[index] }
%{ endfor ~}%{ endfor ~}

  children:
    rally_nodes:
      children:
%{ for cluster, hostinfo in rally_nodes ~}
        rally_nodes_${cluster}:
          hosts:
%{ for index, ip in hostinfo.ips ~}
            ${ip}:
%{ endfor ~}
%{ endfor ~}
    es_nodes:
      children:
%{ for cluster, hostinfo in es_nodes ~}
        es_nodes_${cluster}:
          hosts:
%{ for index, ip in hostinfo.ips ~}
            ${ip}:
%{ endfor ~}
%{ endfor ~}

#
# EOF
#
